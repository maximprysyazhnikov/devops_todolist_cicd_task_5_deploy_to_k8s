name: Deploy Environments

on:
  push:
    branches:
      - main
      - develop

jobs:
  deploy-development:
    uses: ./.github/workflows/deploy-to-kind.yml
    with:
      environment: development

  deploy-staging:
    uses: ./.github/workflows/deploy-to-kind.yml
    with:
      environment: staging
